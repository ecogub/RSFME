RSFME Decisions Log
====================
This file tracks all significant decisions made during the improvement of the paper and codebase.
Update this file whenever a non-trivial choice is made.

---

2026-07-27 | M0 Review Complete
- Decision: Conducted full codebase and paper review as M0.
- Context: Project dormant for >1 year. Needed fresh assessment before making changes.
- Key findings documented in CLAUDE.md under "M0 Review: Areas of Improvement."
- Next step: M1 will define a plan and subsequent milestones collaboratively.

2026-07-27 | Milestone-Driven Development Adopted
- Decision: Use milestone-driven development with CLAUDE.md as living documentation.
- Context: Large number of interrelated code and text improvements needed. Breaking work
  into milestones prevents scope creep and ensures alignment between author and AI assistant.
- M0 = review, M1 = plan, future milestones TBD in M1.

2026-07-27 | Git Permissions Updated for Dropbox Project
- Decision: Added project-specific allow rules to user-level Claude Code settings to enable
  tool access to C:\Users\gubbi\Dropbox\RSFME while keeping broader Dropbox deny rules.
- Context: User settings blocked all Dropbox access. Since the project lives in Dropbox,
  specific exceptions were needed.

2026-07-27 | M1 Plan Written — Three-Phase Improvement Flow
- Decision: Improvement work follows code → figures → text ordering (M2 → M3 → M4).
- Context: Nic reviewed M0 findings and determined that fixing code correctness first is
  essential because figures depend on correct results, and text depends on final figures.
- Detail: M2 is subdivided into data portability (M2a), bug fixes (M2b), refactoring (M2c),
  and verification (M2d). M3 covers figure style and regeneration. M4 covers paper structure
  and text reconciliation. See plans/m1_planning.md for full breakdown.
- Open questions posed to Nic about data file hosting, ts_simulation placement, flowchart
  approach, new analyses, and MacroSheds site-year count discrepancy.

2026-07-27 | M1 Open Questions Resolved
- w3_sensor_wdisch.feather: Added to repo locally but MUST be in .gitignore — data is
  proprietary. Scripts should reference it via here() at a repo-relative path.
- ts_simulation figure (pop_test.png): Goes to supplement, not main text.
- Decision flowchart (Fig 15): Recreate programmatically for easy editing.
- New analyses: OK if they are major improvements in the same conceptual space, but
  prefer light touch. Don't add new methods or datasets without clear justification.
- MacroSheds count: 210k site-years were computed, ~16k passed quality cutoffs for
  flux derivation. Both numbers are correct in their respective contexts — paper should
  clarify this distinction.

2026-07-27 | Workflow Convention Established
- Decision: After every milestone or sub-milestone, follow the pattern:
  do work → update CLAUDE.md → update decisions_made.txt → commit → push
- Context: Documentation updates go BEFORE the commit so everything ships atomically.
  Keeps the project documentation in sync with the code at all times.

2026-07-27 | M2a+M2b Completed
- Replaced 7 hardcoded desktop paths with here(). NEON script uses env var fallback.
- Fixed undefined variables (flag, test), Plynlimon format mismatch (.RData→.csv),
  loop variable shadowing (renamed i→solute_var/coarse_n/k), calculate_truth_ts.R
  global deps (now explicit params) and q_df/q_df_add bug in monthly branch.
- Verified "result accumulation bug" is not a bug — R for-loops are immune to
  loop variable modification. Code is inefficient but correct.
- Error band labels in 2_coarsen_figure.R are visually correct (two swaps cancel).
  Will clean up naming in M2c.

2026-07-27 | M2c Refactoring Completed
- Performance: Moved data read, ARIMA fit, source() call, and function definitions
  outside the rep loop in 1_ts_simulation_analysis.R (was re-doing all 100x per freq).
  Removed debug plot() call from inside rep loop.
- rbind-in-loop: Converted to list accumulation + bind_rows() in 5 scripts:
  ts_simulation, ms_application_compare, coarsen HBEF/Plynlimon/NEON.
- Deprecated ggplot2: size→linewidth in all line geoms (5 files), lwd→linewidth in
  NEON figure.
- Unused libraries: Swept 15 scripts, removed ~60 unused library() calls. Added
  missing RiverLoad/lubridate where needed by sourced flux_methods.R. Added zoo to
  misc_figs.R (needed for na.approx, was getting it transitively from xts).
- Error band labels: Renamed confusing fill keys in 2_coarsen_figure.R to descriptive
  band_5pct/band_20pct with explicit named color/label mapping.
- Bug fix: Plynlimon site_code was hardcoded as 'w3' instead of using the site_code
  variable (should be 'UHF'). Fixed.
- Typo: "Enchanced" → "Enhanced" in 3_descriptive_figures.R.
- Deferred: Large-scale code deduplication (HBEF/Plynlimon/NEON share ~300 lines but
  refactoring is too risky before verification). Shared config.R also deferred.
- Deferred: ts_simulation/defunct/ cleanup (low priority).

2026-07-27 | M2d Verification — Two Bugs Found and Fixed
- Pre-existing bug: 1_ts_simulation_analysis.R line 299 used simulated_series[[4]]
  (chemostatic) instead of [[5]] (no-pattern) for "no pattern / base flow" truth.
  Copy-paste error. Fixed.
- Pre-existing bug: 1_coarsen_analysis.R had missing closing braces for the j and
  coarse_n loops (present in original code since before M2 changes). File would fail
  to parse as a complete R script. Fixed by adding braces matching the Plynlimon
  version's structure.

2026-07-27 | M3a: Unified Figure Style Established
- Created source/plot_theme.R with theme_rsfme(), method/solute/season palettes,
  error band scales, and ggsave_hess() helper.
- Based on HESS submission requirements: 300 DPI, sans-serif (Helvetica), single-col
  8cm / double-col 17.4cm, colorblind-safe (Paul Tol qualitative scheme).
- Method palette: blue=#4477AA (LI), red=#EE6677 (Beale), green=#228833 (Rating),
  yellow=#CCBB44 (Composite), black=#000000 (Truth).
- Error bands: green=#228833 (±5%), yellow=#CCBB44 (±20%).
- Panel labels should use lowercase (a), (b) per HESS style.

2026-07-27 | Code Review Before Push Convention Added
- Decision: Do not push code without running /code-review first for feedback.
- Context: User requested quality control step before changes reach remote.

2026-07-27 | M4 Plan Written and Partial Execution
- Decision: M4 plan created at plans/m4_writing.md. All paper edits go to
  paper/paper_HESS_draft_v2_claude.docx (copy of v2). Original drafts never touched.
- Docx skill installed: read via pandoc or XML extraction, edit via unzip/edit/rezip.
- Completed M4a-1 (heading rename), M4a-2 (figure renumbering 1-12), M4a-4 (Table 1
  verified), M4a-5 (MacroSheds clarification), M4c-1 (typo fixes), M4c-4 (repetition
  analysis).
- Resolved: equations are inline images (not broken OMML), rendering fine in Word.
- Resolved: Figs 7/8/13 were numbering gaps — renumbered sequentially, no content missing.
- Decision: Decision flowchart is now Fig 12 (was Fig 15 before renumbering).
- Remaining M4 work blocked on M2/M3 completion (quantitative claims audit, NEON
  expansion, Conclusions draft, caption updates, final read-through).

2026-07-27 | Repository Restructuring (M5a + M5b)
- Decision: Consolidated all scripts into paper/source/ with numbered naming (01–15),
  all data into data/ subdirectories, all figures into paper/figures/.
- Context: User requested cleaner separation — "all scripts in paper/source, all data
  in /data, all figures in paper/figures. Top-level /source is just for load calcs."
- Data outputs (RData, CSV results) go to data/ subdirs (coarsen_hbef/, coarsen_plynlimon/,
  coarsen_neon/, ts_simulation/), NOT paper/data/ as originally planned in M5b.
- All 18 scripts had internal paths updated. 11 figure scripts verified running clean.
- Old subdirectories under paper/ (coarsen_plot/, ts_simulation/, etc.) still exist with
  original files — pending cleanup after confirming nothing depends on them.

2026-07-27 | Code Deduplication — Coarsening Scripts
- Decision: Extracted ~300 lines of shared coarsening logic from HBEF, Plynlimon, and
  NEON analysis scripts into a new run_coarsening_experiment() function in
  paper/source/coarsen_helpers.R.
- Context: All three scripts had near-identical loops for coarsening time series data,
  computing truth, and applying flux methods. The shared function takes ts_df, site_code,
  target_wy, area, loop_vec, reps, and daily_agg as parameters.
- Each analysis script now ~35 lines (was ~110). The daily_agg parameter handles the
  NEON case where data is already daily (no aggregation needed).
- The function assigns `area` to .GlobalEnv because flux_methods.R references it as a
  free variable — ugly but preserves compatibility with the core flux computation code.

2026-07-27 | M5c+M5d: Runner Script and README Written
- Wrote paper/source/00_run_all.R: sources scripts 01-15 in order, checks packages
  and data files, prints timing, accepts start_from argument for partial runs.
- Rewrote README.md with paper overview, repo structure, reproduction instructions,
  and script run order table. Replaced the outdated directory-based description.

2026-07-27 | Old Paper Subdirectories Cleaned Up
- Deleted old R scripts, figure PNGs, and data copies from 8 paper subdirectories.
- Removed 24GB raw NEON downloads (paper/neon_discussion/data/) and old MacroSheds
  feather files (paper/macrosheds_application/ms_flux_12162022/) — both superseded
  by data/macrosheds/ EDI CSVs.
- Kept: paper/ts_simulation/defunct/ (archived scripts), paper/method_illustration/
  (Fig 1 PNG), paper/flowchart/ (Fig 12 PNGs).

2026-07-27 | M4: Paper Text Updates Applied
- Fixed 3 cross-reference bugs from M4a-2 renumbering in _claude docx:
  "Figures 7 and 10" → "7 and 8" (HBEF), "Figures 9 and 12" → "9 and 10"
  (Plynlimon), "Figures 8 and 12" → "8 and 10" (Ca comparison).
- Applied repetition fix (M4c-4 item 1): removed "defensible" judgment sentence
  from NO3 Results paragraph, keeping the Discussion version.
- Expanded NEON results (M4a-3): replaced 1 paragraph with 3 paragraphs covering
  conductivity (Figures a1-a4), turbidity (Figures a5-a8), and synthesis findings.
- Wrote Summary and Conclusions section (M4a-1): 5 paragraphs covering key findings,
  MacroSheds application, decision framework, and future directions.
- All edits in paper/paper_HESS_draft_v2_claude_final.docx. Original _claude.docx
  preserved (may be locked by Word).
- Remaining M4 items blocked on Nic: "FINAL VERSION LINK" URL, Works Cited
  formatting, quantitative claims audit (needs full re-run), final read-through.

2026-07-27 | M3 Figure Regeneration Complete
- All 11 figure scripts regenerated with unified theme (theme_rsfme(), ggsave_hess()).
- 30 PNGs consolidated in paper/figures/ with descriptive naming: fig01–fig12 for main
  text, figa_neon_* for NEON supplement, figa9_* for MacroSheds supplement,
  fig_supp_ts_simulation for simulation supplement, fig_hydro_regime/fig_cq_regime
  for descriptive panels.
- Analysis scripts (01, 04, 07, 09) are long-running and were not re-run; paths and
  syntax verified but full execution skipped.

2026-07-27 | M4b-1: Quantitative Claims Audit
- Recomputed all quantitative claims from raw data files (HBEF feather, Plynlimon CSV,
  HBEF chemistry CSV, MacroSheds load_annual.csv). Compared against paper text.
- 3 trivial fixes applied directly to paper_HESS_draft_v2_claude_final.docx:
  (1) HBEF yield exponent: 10^10 → 10^8 (was ~100× too large)
  (2) Ca-SpCond slope: 0.0063 → 0.063 (decimal point error, 10× too small)
  (3) Ca sd in Fig 3 caption: 0.22 → 0.23 (to match text and data)
- 4 judgment-call proposals written to plans/m4b_quantitative_audit_proposals.md:
  Issue 3: Ca-SpCond R² (0.92 in paper vs 0.98 in data)
  Issue 4: Plynlimon C:Q slopes/R² completely wrong (Ca slope -0.05 in paper vs -0.26
    in data, R² 0.40 vs 0.82). Narrative about "weaker fits at Plynlimon" is incorrect.
  Issue 5: MacroSheds site/solute/site-year counts don't match current data in any
    section (Abstract, Methods, or Conclusions).
  Issue 6: Plynlimon incomplete days count (20/37 in paper vs 28/40 in data).
- All HBEF descriptive stats verified correct: NO3 mean/sd, Ca mean/sd, Q mean/sd,
  missing days, C:Q slopes and R² values all match.

2026-07-27 | M4b-1: Quantitative Audit — All Fixes Applied
- Nic approved all 4 judgment-call fixes:
  Issue 3: Ca-SpCond R² 0.92→0.98 (go with data)
  Issue 4: Plynlimon C:Q — text was simply incorrect, never matched data. Checked all
    3 sites (CR, LHF, UHF) × all water years (2007-2009): no combination produces
    paper's claimed values. Ca slope -0.05→-0.26, R² 0.40→0.82. Rewrote "both fits
    are less explanatory" to note Ca is similarly strong at both sites.
  Issue 5: MacroSheds counts updated to current data: 26,685 site-years, 146 sites,
    100 solutes (in Abstract, Methods, and Conclusions).
  Issue 6: Plynlimon incomplete days 20/37→28/40.
- Combined with earlier trivial fixes (Issues 1, 2, 7), all 7 audit issues resolved.
- 16 XML edits applied to paper_HESS_draft_v2_claude_final.docx.

2026-07-28 | Code Review Fixes — 10 Findings Addressed
- All 10 code review findings fixed:
  1. nth_element argument swap (coarsen_helpers.R, 06_coarsen_example_figure.R):
     Pre-existing bug — start_pos was used as step-size instead of starting position.
     Fixed: nth_element(ts_df$date, start_pos, n = coarse_n). This changes coarsening
     results — error envelopes now measure phase uncertainty at fixed frequency instead
     of random frequency variation. All coarsening analyses (04, 07, 09) being rerun.
  2. O(n²) inner loop (coarsen_helpers.R): coarse_chem accumulated across iterations.
     Fixed: simplified to single-pass accumulation.
  3. NA composite crash (calculate_annual_flux.R): guarded with is.data.frame() check.
  4. ifelse(method == NA) → explicit is.na() guard.
  5. Unused raw_data_con_in parameter → now used instead of re-filtering domain_chem.
  6. Removed unsafe parallelism suggestion comment.
  7. Undefined good_months → derived from data length.
  8. No-op eval(datecol) → removed eval() wrapper.
  9. rbind in ts_simulation inner loop → list accumulation + bind_rows().
  10. Blanket tryCatch → now uses warning() instead of cat() for visibility.

2026-07-28 | Post-Fix Coarsening Audit — Paper Text Corrections
- Reran audit of all coarsening quantitative claims after nth_element bug fix.
- Key finding: HBEF Ca results changed significantly. Under old buggy code, PW
  appeared to be the best method due to random frequency variation. With corrected
  phase-offset coarsening, composite is clearly the best method (near 0% bias at
  all sub-weekly frequencies), while PW has a persistent +20% positive bias.
- Updated para 146: "linear interpolation performed best" → composite is best,
  consistent with Aulenbach et al. (2016) recommendation.
- Updated para 150: "C:Q model fit does not accurately describe the relationship"
  → composite leverages C:Q fit effectively for Ca; both solutes benefit from
  residual-corrected approaches.
- Conclusions claim (para 208) verified correct as-is: "composite within ~5% at
  weekly for informative C:Q" holds (HBEF Ca median -4.7% at weekly, Plynlimon
  Ca median -1.4%).

2026-07-27 | M5: End-to-End Pipeline Test (incomplete)
- Ran 00_run_all.R. Scripts 01-03 completed successfully. Script 04 (HBEF coarsening,
  100 reps) started but the session was killed before completion.
- Analysis scripts 04, 07, 09 were subsequently rerun individually (with nth_element
  fix applied) and all completed successfully. Figure scripts 05, 06, 08, 10 also
  rerun and regenerated all output PNGs.
- A full uninterrupted 00_run_all.R end-to-end test has not been completed.

2026-07-28 | M2c: Shared Config Extracted
- Decision: Created `source/config.R` with all hardcoded constants (HBEF_AREA, PLYN_AREA,
  site codes, target water years, CA_SPCOND_SLOPE, COARSEN_REPS, HBEF_SOLUTES).
- All 13 scripts updated to `source(here('source/config.R'))` and use named constants.
- NEON script (09) only uses COARSEN_REPS since it dynamically determines area/site/wy.

2026-07-28 | M4b: Figure Caption Audit
- Decision: Audited all figure captions against corrected data (post-nth_element fix).
- 7 fixes applied: 3 method-ranking claims updated (Figs 7, 8, 9), 4 turbidity figure
  labels corrected (Figs a5-a8 said "conductivity" but show turbidity).

2026-07-28 | M6: Figure 11 Panel Improvement
- Decision: Replaced standalone 1:1 scatter with two-panel figure using patchwork.
  Panel A: 1:1 scatter (estimated vs sensor-derived load). Panel B: difference-from-truth
  bar chart (dodged by method, grouped by water year).
- Used shape=21 filled points in scatter so both panels use `fill` aesthetic, enabling
  shared legend via `plot_layout(guides = 'collect')`.
- Caption updated in docx to reference panels (A) and (B).

2026-07-28 | M5c/M8: Struck End-to-End Test, Created M8
- Decision: Struck the 00_run_all.R end-to-end test from the plan (individual scripts
  verified independently; full run is 3+ hours and low value).
- Created M8 "Final Tasks" milestone for Nic-owned items: FINAL VERSION LINK placeholder,
  Works Cited formatting, final read-through. Moved from M4c.

2026-07-28 | M7: Adversarial Code Review
- Decision: Performed a full adversarial review of all source files and analysis scripts.
  Found 25 issues across 4 severity tiers. Fixed 21 of 25:
  - M7a (Critical): 3/4 fixed. hold_factor contamination in simulation chemistry,
    mean_or_x computing variance instead of mean, wyday/enlightened_yday reordering
    EGRET inputs. Self-referential truth is a methodological question (not a code fix).
  - M7b (High): 5/6 fixed. Script 15 intercept, NEON filename parsing, WRTDS tryCatch,
    leap-year scaling, molecular conversion map indexing. Ca~SpCond model inconsistency
    between scripts 13/15 needs Nic's decision.
  - M7c (Medium): 9/9 fixed. No-flow replacement, gap-blanking loop, drain_area_va,
    Plynlimon Q conversion (3 files), convert_unit precedence, non-finite concentrations,
    outlier definition, CI→PI label, SiO2/SiO3 suffix.
  - M7d (Low): 4/6 fixed. base_storm_sep install guard, copy-paste in script 02,
    old_bottom/new_bottom initialization. Global area variable and hardcoded date
    in filenames deferred.
- Key concern: All coarsening analyses (scripts 01, 04, 07, 09) need rerun after these
  fixes to regenerate results and figures. The hold_factor, wyday, Plynlimon Q conversion,
  and non-finite concentration fixes all affect computed values.

2026-07-28 | M7: Ca~SpCond Model Unification
- Decision: Unified Ca~SpCond regression across scripts 13, 04, 05, 14, 15.
  All now use free-intercept model (Ca ~ spCond) on HBEF CSV data (WY 2016, W3).
  Old: origin-forced slope 0.06284158 in scripts 04/05/14, separate MacroSheds-fitted
  model with intercept in script 15.
  New: intercept 0.01283, slope 0.05906, stored as CA_SPCOND_INTERCEPT/CA_SPCOND_SLOPE
  in config.R. Script 15 no longer reads MacroSheds timeseries or fits its own model.

2026-07-28 | M7: Self-Referential Truth — Deliberate Choice
- Decision: Nic confirmed that defining "truth" as the composite method on full-resolution
  data is a deliberate choice. No code change needed.

2026-07-28 | M7: Simulation Filename Cleanup
- Decision: Removed hardcoded date '20221221' from simulation output filenames.
  Scripts 01, 02, 03 now use {freq}Freq_{reps}Reps.csv (no date stamp).
